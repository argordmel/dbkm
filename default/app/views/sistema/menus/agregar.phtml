<?php View::flash(); ?>

<div class="container shell-view">

    <?php View::process($page_module, $page_title, $set_title); ?>

    <?php echo DwForm::open(); ?>

        <div class="row">
            <div class="col-md-4">
                <?php echo DwForm::text('menu.menu', array('class'=>'input-lower input-required mask-alphanum'), NULL, 'Título'); ?>
            </div>
            <div class="col-md-4">
                <?php echo DwForm::dbSelect('menu.menu_id', 'menu', array('sistema/menu', 'getListadoMenu', Menu::ACTIVO), 'Ninguno', array('class'=>'input-lower mask-pints'), NULL, 'Menú padre'); ?>

            </div>
            <div class="col-md-4">
                <?php echo DwForm::dbSelect('menu.recurso_id', 'recurso', array('sistema/recurso', 'getListadoRecurso', Recurso::ACTIVO), 'Ninguno', array('class'=>'input-lower mask-pints'), NULL, 'Recurso'); ?>
            </div>
        </div>
    
        <div class="row">
            <div class="col-md-4">
                <?php echo DwForm::text('menu.posicion', array('class'=>'mask-pint'), NULL, 'Posición'); ?>
            </div>
            <div class="col-md-4">
                <?php echo DwForm::text('menu.icono', array('class'=>'input-lower mask-alphanum'), NULL, 'Icono a mostrar'); ?>
            </div>
            <div class="col-md-4">
                <?php echo DwForm::text('menu.url', array('class'=>'input-required input-lower'), NULL, 'URL'); ?>
            </div>
        </div>
    
        <div class="row">
            <div class="col-md-4">
                <?php echo DwForm::select('menu.visibilidad', array(Menu::BACKEND=>'Backend', Menu::FRONTEND=>'Frontend'), array('class'=>'input-lower input-requireds'), Menu::BACKEND, 'Visibilidad'); ?>
            </div>
        </div>
    
        <div class="row">
            <div class="form-actions">
                <?php echo DwButton::save('Guardar menú'); ?>
                <?php echo DwButton::reset(); ?>
                <?php echo DwButton::cancel(); ?>
            </div>
        </div>

    <?php echo DwForm::close(); ?>

</div>

<script type="text/javascript" >
    $(function(){
        $("#menu_recurso_id").on('change',function(){
            var recurso = $('option:selected',this).text();
            $("#menu_url").val(recurso.replace('/*','/'));
        });
    });
</script>